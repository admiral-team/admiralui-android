<?xml version="1.0" encoding="utf-8"?>
<!--
     AdmiralUI
     Generated by Fastlane Figma Plugin.
-->
<resources>
    <attr name="admiralTextStyle" format="enum">{% for style in styles %}
        <enum name="{{ style.name_info.style_name | remove: "_" | remove: " " | downcase}}" value="{{ forloop.index }}" />{% endfor %}
        <enum name="pinNum" value="{{ styles.size | plus: 1}}" />
    </attr>
    <attr name="admiralTitleTextStyle" format="enum">{% for style in styles %}
        <enum name="{{ style.name_info.style_name | remove: "_" | remove: " " | downcase}}" value="{{ forloop.index }}" />{% endfor %}
        <enum name="pinNum" value="{{ styles.size | plus: 1}}" />
    </attr>
    <attr name="admiralSubtitleTextStyle" format="enum">{% for style in styles %}
        <enum name="{{ style.name_info.style_name | remove: "_" | remove: " " | downcase}}" value="{{ forloop.index }}" />{% endfor %}
        <enum name="pinNum" value="{{ styles.size | plus: 1}}" />
    </attr>
    <attr name="admiralSubtitleSecondTextStyle" format="enum">{% for style in styles %}
        <enum name="{{ style.name_info.style_name | remove: "_" | remove: " " | downcase}}" value="{{ forloop.index }}" />{% endfor %}
        <enum name="pinNum" value="{{ styles.size | plus: 1}}" />
    </attr>
</resources>