package com.admiral.themes

/**
 * AdmiralUI
 * Generated by Fastlane Figma Plugin.
 */

enum class ColorPaletteEnum { {% for style_name in common_style_names %}
    {{ style_name | snakecase | replace: '/', '_' | upcase }}{% if forloop.last == false %},{% else %};{% endif %}{% endfor %}

    companion object {
        fun from(index: Int?) = if (index == null) null else values()[index]

        @Suppress("LongMethod")
        fun ColorPaletteEnum?.colorResToToken(): Int? {
            return when (this) { {% for style_name in common_style_names %}
                {{ style_name | snakecase | replace: '/', '_' | upcase }} -> ThemeManager.theme.palette.{{ style_name | default_format }}{% endfor %}
                else -> null
            }
        }
    }
}