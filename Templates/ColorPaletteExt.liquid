package com.admiral.demo.features.home.colors

import com.admiral.themes.ColorPalette
import com.admiral.uikit.ext.hex

/**
 * AdmiralUI
 * Generated by Fastlane Figma Plugin.
 */
@Suppress("LongMethod")
fun ColorPalette.colors(): List<ColorModel> {
    return listOf({% for style_name in common_style_names %}
        ColorModel(name = "{{ style_name | default_format }}", color = {{ style_name | default_format }}, hex = {{ style_name | default_format }}.hex()),{% endfor %}
    )
}