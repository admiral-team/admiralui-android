<?xml version="1.0" encoding="utf-8"?>
<!--
     AdmiralUI
     Generated by Fastlane Figma Plugin.
-->
<resources>{% for group in groups %}{% for style in group.styles %}
    <color name="{{ group.name | default_format }}_{{ style.name_info.style_name | default_format }}">{{ style.color.hex_with_alpha }}</color>{% endfor %}{% endfor %}
</resources>